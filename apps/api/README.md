# API service (`apps/api`)

FastAPI skeleton for the Space Biology Evidence Engine.

```bash
# from repo root
make api
# GET http://localhost:8000/health
```

Package import path: `spacebio_api` (source under `apps/api/src/spacebio_api`).
