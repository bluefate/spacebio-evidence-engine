{
  "name": "spacebio-evidence-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root for the Space Biology Evidence Engine.",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @spacebio/web",
    "build:web": "npm run build -w @spacebio/web",
    "lint:web": "npm run lint -w @spacebio/web"
  },
  "engines": {
    "node": ">=22"
  }
}
